# Data 目录整理实施计划

> 对应设计：`docs/superpowers/specs/2026-07-30-data目录整理设计.md`

## 任务 1：整理文件结构

1. 将 `data/eval/sft_v2_json/hard_mini_dev_en.jsonl` 移至 `data/eval/mini_dev.jsonl`。
2. 将 fast、full 和总 manifest 上移至 `data/eval/`。
3. 删除旧 mini、混合语言 hard mini 及其 manifest。
4. 将 `data/rl/smoke_v1_768/` 中三个文件上移至 `data/rl/`。
5. 删除 `data/sft/v3_json/` 和 `data/.DS_Store`。

## 任务 2：更新元数据

1. 重写 `data/eval/manifest.json`，只描述 mini、fast、full。
2. 更新 `data/rl/manifest.json` 中的来源和输出路径。
3. 更新 `data/sft/v3_real_json/manifest.json` 与 `audit.json`，将 331 条增量的来源改为权威 Teacher Rollout 和转换脚本。
4. 冻结 2026-07-06 Teacher 转换使用的 system prompt，确保 331 条样本可逐字节复现。

## 任务 3：补充目录文档

1. 重写 `data/README.md`。
2. 新建 `data/teacher_rollouts/README.md`。
3. 新建 `data/sft/README.md`。
4. 新建 `data/eval/README.md`。
5. 新建 `data/rl/README.md`。

## 任务 4：修正调用方

1. 更新 SFT、eval、RL 脚本中的旧默认路径。
2. 更新根 README、训练文档和脚本 README 中的命令示例。
3. 检索并处理所有旧目录引用。

## 任务 5：验证

1. 检查 JSON 和 JSONL 可解析性。
2. 确认 eval 行数为 110/120/300，RL 行数为 768/60。
3. 确认最终 SFT 为 5817 条，并包含 331 个 Teacher 成功样本。
4. 检查旧路径引用只允许存在于历史设计说明中。
5. 审查 `git diff --check` 和最终变更清单。
