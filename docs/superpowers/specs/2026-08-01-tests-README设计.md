# tests README 设计

## 目标

为 `tests/` 增加一份面向使用者的简洁说明。

## 内容

README 只包含两部分：

1. **文件作用**：说明当前 6 个 `test_*.py` 分别验证的功能。
2. **使用方式**：说明如何安装开发依赖，以及运行全部测试、单个测试文件和单个测试用例。

## 边界

- 不介绍复杂测试架构、fixture 设计或 CI 流程。
- 不记录内部整修、迁移或历史版本。
- 命令必须能够从仓库根目录直接执行。
